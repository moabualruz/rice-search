# Rice Search - Unified Inference Configuration
# Models are managed by the unified-inference service (SGLang-based)

# =============================================================================
# UNIFIED INFERENCE URL
# =============================================================================
INFERENCE_URL=http://unified-inference:3001

# =============================================================================
# MODEL NAMES (configured in unified-inference service)
# =============================================================================

# Dense Embeddings - Sentence Transformers
# Supported: jinaai/jina-embeddings-v3, BAAI/bge-base-en-v1.5, intfloat/e5-large-v2
EMBEDDING_MODEL=jinaai/jina-embeddings-v3

# Sparse Model (keyword-based fallback)
SPARSE_MODEL=bm25

# Reranking Model - Cross-Encoder
# Supported: BAAI/bge-reranker-base, jinaai/jina-reranker-v2-base-multilingual
RERANK_MODEL=BAAI/bge-reranker-base

# LLM for RAG Chat & Query Classification - vLLM
LLM_MODEL=google/codegemma-7b-it

# =============================================================================
# EMBEDDING DIMENSION
# =============================================================================
# Must match the model's output dimension:
# - jina-embeddings-v3: 1024
# - bge-base-en-v1.5: 768
# - e5-large-v2: 1024
EMBEDDING_DIM=1024

# =============================================================================
# RERANKING MODE
# =============================================================================
# "rerank" = Use unified-inference rerank endpoint (cross-encoder model)
# "llm" = Use LLM prompting for reranking (no extra model needed)
RERANK_MODE=rerank
